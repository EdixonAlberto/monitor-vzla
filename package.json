{
  "name": "monitor-vzla",
  "description": "Monorepo con diferentes clientes para monitorear el precio del dolar paralelo en Venezuela",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "author": {
    "name": "Edixon Piña",
    "email": "edixonalbertto@gmail.com"
  },
  "scripts": {
    "format": "prettier --write packages/**/*.ts build/*.js",
    "clean": "rimraf dist packages/core/dist",
    "build": "node build",
    "start": "node dist",
    "dev": "yarn clean && yarn core:build && yarn",
    "core:build": "yarn workspace @monitor/core build",
    "bot-discord": "yarn workspace bot-discord",
    "bot-telegram": "yarn workspace bot-telegram"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@edixon/concord": "^2.0.0",
    "socket.io-client": "^4.5.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "prettier": "^2.6.2",
    "rimraf": "^3.0.2",
    "tsc-alias": "^1.6.6",
    "typescript": "^4.6.3"
  },
  "engines": {
    "node": ">=16.6.0",
    "yarn": "~1.22.0"
  }
}