{
  "name": "bot-monitor-vzla",
  "description": "Bot de discord para monitorear el dolar paralelo en Venezuela",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --config",
    "start": "node dist"
  },
  "nodemonConfig": {
    "ext": ".ts,.env",
    "watch": [
      "src/*"
    ],
    "verbose": false,
    "exec": "yarn build && yarn start"
  },
  "dependencies": {
    "@edixon/concord": "^1.5.2",
    "instagrapi": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "typescript": "^4.6.3"
  },
  "engines": {
    "node": ">=14.18.2",
    "yarn": "~1.22.0"
  }
}
